Update my gmail.js for the ReplyMate Chrome extension.

Current behavior:
- On Gmail message row hover, an "AI Reply" button appears
- The button is currently positioned near the far right side of the row
- This can overlap or cover Gmail's built-in hover action icons such as snooze, mark as read, archive, delete, etc.

New goal:
Improve the hover button placement so that the "AI Reply" button does NOT cover Gmail's built-in action icons.

Requirements:
1. When possible, detect the Gmail hover action icon container inside the message row
2. Insert the "AI Reply" button just to the LEFT of Gmail's existing hover action icons
3. Do not cover or block existing Gmail buttons
4. If the action icon container cannot be found, fall back to the current safe positioning approach
5. Keep the existing hover behavior and workflow unchanged
6. Keep the existing AI Reply click behavior unchanged
7. Use plain JavaScript
8. Add comments explaining the logic
9. Avoid duplicate AI Reply buttons

Important:
- Different Gmail rows may show slightly different built-in action buttons
- Make the placement adaptive instead of relying on only one rigid selector
- Prefer inserting into an existing action-button area rather than absolute-positioning over the row